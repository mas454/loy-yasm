(macro (lcase exp *body)
       `(begin
	 (rasm "case ")
	 ,exp
	 ,@(map (lambda (con)
		  `(begin
		    (rasm "when ")
		    ,(car con)
		    ,@(cdr con))) (a2l body))
	 (rasm "\nend")))

(lcase 10
      (0
       (puts "hello"))
      (10
       (puts "hello")
       (puts "ok!")))